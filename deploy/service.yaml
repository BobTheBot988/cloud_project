apiVersion: v1
kind: Service
metadata:
  name: llm-proxy
  namespace: default
  labels:
    app: llm-proxy
spec:
  type: NodePort
  selector:
    app: llm-proxy
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
